jar {
    archiveBaseName = "test-support"
    archiveVersion = "0.0.1-SNAPSHOT"
}

bootJar.enabled = false
jar.enabled = true

dependencies {

    implementation("org.springframework.boot:spring-boot-starter-data-jpa")

    implementation project(":main-module")
	
}